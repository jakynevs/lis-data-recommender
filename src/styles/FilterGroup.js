import styled from 'styled-components';

const FilterGroup = styled.div`
  display: flex;
  flex-direction: column;
  gap: 10px; // Adjusts the space between items within the group
`;

export default FilterGroup;